import 'package:flutter/material.dart';

class Commennts extends StatelessWidget {
  const Commennts({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title:const Text('comments page'),),
    );
  }
}